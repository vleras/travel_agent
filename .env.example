# Optional — without this key the agent uses curated city attractions (still fully functional)
VITE_GEMINI_API_KEY=

# Optional — Unsplash Access Key for place photos (50 req/hour demo apps).
# Without it, photo fetch skips Unsplash and uses Wikipedia → Commons.
VITE_UNSPLASH_ACCESS_KEY=
